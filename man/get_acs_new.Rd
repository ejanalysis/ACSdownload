% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_acs_new.R
\name{get_acs_new}
\alias{get_acs_new}
\title{Get full USA ACS data by table and fips (table-based summary file format)}
\usage{
get_acs_new(
  tables = ejscreen_acs_tables,
  fips = "blockgroup",
  yr = acsdefaultendyearhere,
  fiveorone = "5",
  return_list_not_merged = TRUE,
  cache_dir = getOption("ACSdownload.cache_dir", NULL),
  timeout_sec = getOption("ACSdownload.timeout", 300),
  max_retries = getOption("ACSdownload.retries", 3),
  parallel = FALSE,
  variables = NULL,
  keep_moe = TRUE,
  keep_annotations = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{tables}{vector of ACS data table codes like "B01001" or "C17002".
Some EJSCREEN tables are only published at tract resolution (notably
"C16001" for detailed languages spoken at home and "B18101" for
disability); when \code{return_list_not_merged = FALSE} and \code{fips = "blockgroup"}, those tables filter to zero rows and are dropped from
the merge with a warning.}

\item{fips}{one of:
\itemize{
\item a single geography type name: "blockgroup", "tract", "county",
"state", "city", "REGION", "MSA", "CSA", "Urban Area",
"Congressional District", "ZCTA", or
"American Indian Area/Alaska Native Area/Hawaiian Home Land"
(only "blockgroup", "tract", "county", "state", "city" are well-tested);
\item a vector of fips codes (all the same width / geography type);
\item \code{NULL} for no row filtering.
}}

\item{yr}{end year of the 5-year ACS summary file (e.g. 2024 for the
2020-2024 survey released by Census Bureau Jan. 2026). Validated
against \code{acsfirstyearavailablehere} (the floor) and \code{current_year + 1}
(the ceiling); bad values fail before any HTTP call.}

\item{fiveorone}{1 or 5; only the 5-year sample is tested here.}

\item{return_list_not_merged}{if \code{TRUE} (the default) return a named
list with one data.table per requested table; if \code{FALSE}, merge all
tables on \code{fips} and return a single data.table.}

\item{cache_dir}{optional path to a directory in which to persist the
downloaded .dat files. The table-based SF files are immutable once a
vintage ships, so caching is safe and dramatically speeds up repeat
calls. Defaults to \code{getOption("ACSdownload.cache_dir", NULL)}.}

\item{timeout_sec}{per-request timeout in seconds for each .dat
download. Defaults to \code{getOption("ACSdownload.timeout", 300)}.}

\item{max_retries}{number of retry attempts after the first for
transient HTTP failures (HTTP 429 and 5xx). Defaults to
\code{getOption("ACSdownload.retries", 3)}.}

\item{parallel}{if \code{TRUE} and both \code{future} and \code{future.apply} are
installed, fetch the .dat files concurrently. The caller must set a
\code{future::plan()} first.}

\item{variables}{optional character vector of specific estimate variable
codes to keep, like \code{c("B25034_001", "B25034_002")}. Names are matched
in the post-rename form (no \verb{_E} infix). When NULL (the default), all
estimate columns from the requested tables are kept.}

\item{keep_moe}{if \code{TRUE} (the default), keep the margin-of-error
columns (\verb{<table>_M<nnn>}); if \code{FALSE}, drop them.}

\item{keep_annotations}{if \code{TRUE}, keep the annotation columns
(\verb{<table>_EA<nnn>}, \verb{<table>_MA<nnn>}) that some vintages include;
the default \code{FALSE} drops them.}

\item{quiet}{if \code{FALSE} (the default) and the \code{cli} package is
installed, show a progress bar over the (sequential) downloads.
Ignored when \code{parallel = TRUE}.}
}
\value{
a named list of data.tables (one per table) or a single merged
data.table, each with \code{GEO_ID}, \code{fips}, \code{SUMLEVEL}, and the estimate
(\verb{<table>_<nnn>}) and margin-of-error (\verb{<table>_M<nnn>}) columns from
the Census file. Estimate column names have the leading "_E" stripped
for compatibility with \code{EJAM::formulas_ejscreen_acs$formula}.
}
\description{
Downloads ACS 5-year data for the requested tables and either a single
geography type (e.g. "blockgroup") or a vector of specific fips codes.
Returns either a list of one data.table per table (the default) or a
single merged data.table.
}
\details{
Targets the Census Bureau "table-based" summary file format introduced
for the 2018-2022 5-year survey (released December 2023) and used since.
See \url{https://www.census.gov/programs-surveys/acs/data/summary-file.html}.

Downloads are made resilient by an httr2 retry/timeout layer
(\code{R/utils-download.R}); set \code{cache_dir} to persist files across calls
(recommended: \code{tools::R_user_dir("ACSdownload", "cache")}). With
\code{parallel = TRUE}, downloads run via \code{future.apply::future_lapply()} --
set a \code{future::plan()} (e.g. \code{future::plan(future::multisession, workers = 4)}) before calling.
}
\examples{
 \dontrun{
   x <- get_acs_new(yr = 2024, tables = "B25034", fips = "county")
   x[["B25034"]][1:3, 1:5]

   # Full EJSCREEN block-group pull (excluding the two tract-only tables):
   bg <- get_acs_new(
     yr = 2024,
     fips = "blockgroup",
     tables = setdiff(ejscreen_acs_tables, c("C16001", "B18101")),
     return_list_not_merged = FALSE,
     cache_dir = tools::R_user_dir("ACSdownload", "cache")
   )
 }

}
