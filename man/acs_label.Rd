% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labels.R
\name{acs_label}
\alias{acs_label}
\title{Look up the human-readable label for one or more ACS variable codes}
\usage{
acs_label(variable_id)
}
\arguments{
\item{variable_id}{character vector of ACS variable codes (e.g.
\code{"B01001_001"}). The trailing margin-of-error variant
(\code{"B01001_M001"}) is automatically normalized to the estimate
variable, so MOE columns get their estimate's label.}
}
\value{
a data.table with one row per input, columns:
\code{variable_id} (as supplied), \code{table_id}, \code{label}, \code{table_title},
\code{universe}. Unmatched codes get NA in the lookup columns.
}
\description{
Returns a data.table joining the input codes to the table title, label,
and population universe from \code{acs_table_shells}. Useful for turning
the cryptic column names that \code{get_acs_new()} returns (e.g.
\code{"B01001_001"}) into something a reader can understand.
}
\details{
The shipped lookup is built from the 2022 5-year vintage of the Census
table shells. Labels for tables that exist across multiple vintages are
stable; for unfamiliar tables, double-check the official documentation
at \url{https://data.census.gov/}.
}
\examples{
 acs_label(c("B01001_001", "B01001_M001", "C16001_002"))

}
